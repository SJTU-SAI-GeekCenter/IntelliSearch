# 完成后端配置

我实现了一个 MCP Search 的 chatbot，现在请你完成下面的任务：

- 重构 `backend/main.py` 的内容 现在这个代码文件太长了
- 前后端耦合，我希望你把对应的前后端接口联通，并且可以在 scripts 文件夹中设置启动命令，把前后端服务启动可以在本地运行网页
    - 如果可以的话，我觉的后端你可以使用 fastapi 的框架
    - **暂时先做一个不流式输出的版本**，注意 请你在前端也加上对应的特效（比如智能体在调用工具时候有一些酷炫的特效等等）

    